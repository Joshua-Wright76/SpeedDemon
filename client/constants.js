export const DEFAULT = 'DEFAULT';
export const LOW_RES = 'LOW_RES';
export const DARK_MODE = 'DARK_MODE';
export const GREYSCALE = 'GREYSCALE';
export const BY_SPEED = 'BY_SPEED';
export const SIMPLIFIED = 'SIMPLIFIED';

/*
for(let coord of trip.coords){
    switch(this.props.mode){
        default:
            path.push({lng: coord.lng, lat: coord.lat});
        case types.LOW_RES:
            path.push({lng: Math.round(coord.lng / 0.005) * 0.005, lat: Math.round(coord.lat / 0.005) * 0.005})
        case types.AVERAGED:
            this.props.setPoint(coord)
            path.push({lng: coord.lng, lat: coord.lat});
    }
}

*/